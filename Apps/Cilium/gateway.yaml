apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: lab-gateway
  namespace: gateway 
  annotations:
    cert-manager.io/cluster-issuer: vault-issuer
    argocd.argoproj.io/sync-wave: "4"
spec:
  gatewayClassName: cilium 
  listeners:
  # --- NEW: Catch plain HTTP traffic on port 80 ---
  - name: http
    protocol: HTTP
    port: 80
    hostname: "*.lab.internal"
    allowedRoutes:
      namespaces:
        from: All
        
  # --- EXISTING: Handle secured HTTPS traffic on port 443 ---
  - name: https
    protocol: HTTPS
    port: 443
    hostname: "*.lab.internal"
    allowedRoutes:
      namespaces:
        from: All
    tls:
      mode: Terminate
      certificateRefs:
      - name: lab-wildcard-tls